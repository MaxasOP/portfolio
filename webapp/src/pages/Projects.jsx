import React from 'react'

export default function Projects() {
  return (
    <main>
      <section className="hero">
        <div className="container hero-grid">
          <div className="hero-copy reveal">
            <div className="eyebrow">Stages</div>
            <h1 className="hero-title">Selected builds from the map.</h1>
            <p className="hero-lead">Three projects, each with its own objective, from shipped builds to the current boss fight.</p>
          </div>
        </div>
      </section>
    </main>
  )
}
