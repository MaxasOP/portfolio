import React from 'react'

export default function Contact() {
  return (
    <main>
      <section className="hero">
        <div className="container hero-grid">
          <div className="hero-copy reveal">
            <div className="eyebrow">Warp pipe</div>
            <h1 className="hero-title">Need a sharper build? Let’s launch it.</h1>
            <p className="hero-lead">Open for internships, collaborations, and product-focused UI work where taste and execution both matter.</p>
            <a className="button primary" href="mailto:manaschauhan.ms@gmail.com">Email me</a>
          </div>
        </div>
      </section>
    </main>
  )
}
