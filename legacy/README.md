# Manas Chauhan — Portfolio (Legacy)

This folder is a backup of the site's static version before migrating to a React/Vite app. Keep these files for reference and incremental porting.

## Contents
- `maxasop-portfolio.html`, `about.html`, `skills.html`, `projects.html`, `contact.html` — themed static pages.
- `super-maxas.css`, `super-maxas.js` — shared theme styles and interactions.
- `server.js` — local test server used during development.
- `package.json` — minimal dependency list.

Do not delete this folder. Use it as the source-of-truth while scaffolding and porting to `webapp/`.
